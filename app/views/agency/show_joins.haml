.panel.panel-default
  .panel-heading
    %h1= link_to @agency.bname, agency_path(@agency)
  .panel-body
    = paginate @agencies

    = render 'map', title: "已备案加盟经纪机构(#{@agencies.count})"

#data_holder{'data-json'=>"#{raw @json.to_json}"}
  
-content_for :head do 
  = render '/shared/gmap'
  = javascript_include_tag 'agency'
